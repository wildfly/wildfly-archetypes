${artifactId}=${artifactId} subsystem
${artifactId}.add=Adds the ${artifactId} subsystem
${artifactId}.remove=Removes the ${artifactId} subsystem
${artifactId}.tick=Specifies how often the ${artifactId} displays a message
${artifactId}.executor=Name of the Jakarta EE Concurrent managed executor service
${artifactId}.deployments=Number of deployments in the server